const ftoc = function (degF) {
  result = (degF - 32) * (5 / 9);
  return Math.round(result * 10) / 10;
};

const ctof = function (degC) {
  result = degC * (9 / 5) + 32;
  return Math.round(result * 10) / 10;
};
